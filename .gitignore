/target
# Scratch build dir used for integration checks. 12G of artifacts that
# must never reach a commit.
/target-integ
.superpowers/
evals/workspace_*/
evals/results/
__pycache__/
evals/**/__pycache__/
evals/**/*.pyc
scratchpad/
.env
.env.*

# Local-only reference material (not redistributed — see NOTICE.md)
/reference/

# LaTeX intermediates from the paper build (docs/paper/Makefile cleans
# these itself; this covers interrupted builds). The PDF is committed.
docs/paper/*.aux
docs/paper/*.log
docs/paper/*.out
docs/paper/*.bbl
docs/paper/*.blg
docs/paper/zorp-paper.tex
