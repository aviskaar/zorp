#!/usr/bin/env bash
"""true" 2>/dev/null; exec python3 "$0" "$@"; exit
"""

# This file intentionally left empty — makes evals/harbor a Python package
# so test_smoke.py can import quecto_agent as evals.harbor.quecto_agent
